{
  "name": "cryptotetes",
  "version": "1.0.0",
  "description": "A dummy ERC20 token",
  "scripts": {
    "compile": "npx truffle compile",
    "clean": "rimraf build",
    "deploy:local": "npx trufle migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "José Bujalance",
  "license": "ISC",

  "dependencies": {
    "@openzeppelin/contracts": "^4.3.1"
  },

  "devDependencies": {
    "rimraf": "^3.0.2",
    "truffle": "^5.4.8"
  }

}
